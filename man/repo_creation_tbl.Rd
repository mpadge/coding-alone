% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-commit-activity.R
\name{repo_creation_tbl}
\alias{repo_creation_tbl}
\title{Monthly repo-creation rate, by source}
\usage{
repo_creation_tbl(
  issue_authors_tbl,
  repo_tbl,
  source_name,
  window = 12L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{source_name}{One of \code{repo_tbl$source} (\code{"pypi"}, \code{"npm"}, \code{"joss"},
\code{"ropensci"}).}

\item{window}{Trailing aggregation window, in months. Default 12: each
reported month's \code{n_metric}/\code{n_repo_months} sum that month and the
preceding 11.}

\item{date_start, date_end}{Date bounds on the analysis window;
\code{date_end} defaults to the start of the current month.}
}
\value{
A tibble with one row per month: \code{month}, \code{n_created}
(\code{window}-month trailing sum of repos created that month). Carries
\code{window} and \code{source_name} as attributes.
}
\description{
Counts how many repositories were created (per GitHub's own
\code{repo_created_at} timestamp, as recorded on \code{issue_authors_tbl}) each
calendar month, for one source, reported as a \code{window}-month trailing
sum in the same way every other rate in this package is - the
ecosystem's own raw growth in repo count over time, meant to be read
alongside \code{commit_rate_tbl()}'s per-repo-month commit rate (see
\code{plot_commit_rate()}) so a reader can judge how much of any shift in
commit rate reflects more repos existing now rather than a change in
per-repo behaviour. Restricted to the same repo population as
\code{commit_rate_tbl()} (repos with a non-\code{NA} popularity metric and a
known creation date), so the two panels describe the same set of
repositories.
}
\examples{
\dontrun{
rc <- repo_creation_tbl (issue_authors_tbl, repo_tbl, "pypi")
}
}
