% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download-data.R
\name{download_repo_data}
\alias{download_repo_data}
\title{Download the \verb{repo-data-out/} dataset from this package's GitHub
release, if it doesn't already exist locally}
\usage{
download_repo_data(
  out_dir = "repo-data-out",
  repo = "mpadge/coding-alone",
  tag = "v0.1"
)
}
\arguments{
\item{out_dir}{Directory to create and download data into. Default
\code{"repo-data-out"}, matching every other function in this package that
reads from it.}

\item{repo}{GitHub \code{owner/repo} to download release assets from.}

\item{tag}{Release tag holding the data assets.}
}
\value{
\code{out_dir}, invisibly.
}
\description{
If \code{out_dir} already exists, nothing is downloaded and the function
returns invisibly without contacting GitHub at all - this is meant to
be safe to call at the start of any analysis script without
re-downloading a dataset that's already there. Otherwise, \code{out_dir} is
created and every asset attached to the given release \code{tag} is
downloaded into it via \code{piggyback::pb_download()}.
}
\examples{
\dontrun{
download_repo_data ()
}
}
