% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-activity.R
\name{num_comments_step_change_tbl}
\alias{num_comments_step_change_tbl}
\title{Step-change in comment volume, from a reference month to now}
\usage{
num_comments_step_change_tbl(
  issue_authors_tbl,
  repo_tbl,
  sources,
  window = 12L,
  ref_date = as.Date("2021-01-01")
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{sources}{Character vector of \code{source_name} values to include (see
\code{issue_rate_tbl()}).}

\item{ref_date}{Reference \code{Date} (first-of-month) to compare against the
latest available month.}

\item{contrib_threshold, window}{Passed to \code{issue_rate_tbl()}.}
}
\value{
A tibble: \code{source}, \code{popularity_stratum}, \code{rate_ref},
\code{rate_latest}, \code{step_change}, \code{latest_month}.
}
\description{
The \code{issue_rate_tbl(metric = "comments")} analogue of
\code{author_density_step_change_tbl()}: for each of several sources, compare
each popularity stratum's comment rate (\code{n_comments}, normalized by
repo-months of exposure) at a fixed reference month against its most recent
value, estimated from linear regression rates.
}
\examples{
\dontrun{
fc <- num_comments_step_change_tbl (issue_authors_tbl, repo_tbl, c ("cran", "npm"))
plot_step_change (fc, SOURCE_DISPLAY_NAME, metric = "comments") +
    ggplot2::labs (title = "Comment volume")
}
}
