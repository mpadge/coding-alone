% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{step_change_tbl}
\alias{step_change_tbl}
\title{Fold-change in non-core rate, from a reference month to now}
\usage{
step_change_tbl(
  issue_authors_tbl,
  repo_tbl,
  sources,
  metric = "issues",
  contrib_threshold = 0.01,
  window = 12L,
  ref_date = as.Date("2021-01-01")
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{sources}{Character vector of \code{source_name} values to include (see
\code{issue_rate_tbl()}).}

\item{metric, contrib_threshold, window}{Passed to \code{issue_rate_tbl()}.}

\item{ref_date}{Reference \code{Date} (first-of-month) to compare against the
latest available month.}
}
\value{
A tibble: \code{source}, \code{popularity_stratum}, \code{rate_ref},
\code{rate_latest}, \code{step_change} (\code{rate_latest / rate_ref}), \code{latest_month}.
}
\description{
For each of several sources, compare each popularity stratum's trailing
12-month rate at a fixed reference month against its most recent value.
A fixed reference month (rather than each stratum's own peak) is used
deliberately: individual per-stratum peaks are noisy, especially for
sparser low-popularity strata where a single active month can dominate a
small repo-month denominator, so anchoring on one shared calendar month
both avoids cherry-picking and keeps strata/sources comparable.
}
\examples{
\dontrun{
fc <- step_change_tbl (issue_authors_tbl, repo_tbl, c ("cran", "npm"))
plot_step_change (fc)
}
}
