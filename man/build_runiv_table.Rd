% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-data-runiverse.R
\name{build_runiv_table}
\alias{build_runiv_table}
\title{Extract repo data for an r-universe}
\usage{
build_runiv_table(universe = c("ropensci", "cran"))
}
\arguments{
\item{universe}{Which r-universe to query: \code{"ropensci"} (default, the
rOpenSci r-universe) or \code{"cran"} (the CRAN mirror r-universe). Only the
rOpenSci r-universe carries software-review metadata, so \code{reviewed}/
\code{review_id} are only present in the result when \code{universe == "ropensci"}.
Also, \code{RemoteUrl} is only a meaningful repo-URL candidate for packages
actually hosted on an r-universe (as \code{ropensci} packages are); for
\code{"cran"}, where it instead reflects CRAN's own build infrastructure, it
is excluded from the URL search.}
}
\value{
A tibble with one row per r-universe package: \code{package},
\code{repo_url}, \code{downloads} (\verb{_downloads$count}, last-month CRAN downloads),
and \code{stars} (\verb{_stars}, GitHub stargazer count); plus, for
\code{universe == "ropensci"} only, a \code{reviewed} flag
(\verb{_metadata$review$status == "reviewed"}) and, for those, the
\code{review_id} of its rOpenSci software review (\code{NA} otherwise).
}
\description{
Build a (package, title, package_url, repo_url) table for every
package currently published in a given r-universe.
}
\examples{
\dontrun{
runiv_tbl <- build_runiv_table ("ropensci")
}
}
