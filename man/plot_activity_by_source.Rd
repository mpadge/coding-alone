% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse-activity.R
\name{plot_activity_by_source}
\alias{plot_activity_by_source}
\title{Compare monthly issue rate across all four sources (\code{pypi}, \code{npm},
\code{joss}, \code{ropensci}), for one popularity stratum. Note that "stratum" is
relative to each source's own distribution (see \code{issue_rate_tbl()}/
\code{popularity_strata()}) - e.g. pypi's Q4 download count and joss's Q4
star count aren't the same absolute popularity, just each source's own
top quarter. A source with no data yet for the requested window (e.g.
not fully fetched - see \code{analysis-plan.md}) just contributes no line,
rather than erroring.}
\usage{
plot_activity_by_source(
  issue_authors_tbl,
  repo_tbl,
  stratum,
  n_strata = 4L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL,
  relative = TRUE,
  start_year = NULL
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{stratum}{Integer popularity stratum to compare (\code{1} = lowest
popularity, \code{n_strata} = highest), matching one of \code{issue_rate_tbl()}'s
\code{popularity_stratum} levels (\code{"Q<stratum>"}).}

\item{n_strata, date_start, date_end}{Passed to each source's
\code{issue_rate_tbl()} call; must be the same \code{n_strata} \code{stratum} is a
level of.}

\item{relative}{If \code{TRUE} (default), rescale each source by its own
mean before plotting - sources sit on very different absolute rate
scales (e.g. pypi's raw issue traffic dwarfs ropensci's), which would
otherwise squash the smaller sources' trends to flat lines near zero.
Puts every line at a comparable "around 1 = that source's own average"
scale, so trends are comparable even though absolute rates aren't. Set
\code{FALSE} to plot absolute rates instead.}

\item{start_year}{Optional year (e.g. \code{2018}) to start the plotted
window from; \code{NULL} (default) plots the full \code{date_start}-\code{date_end}
window.}
}
\value{
A ggplot object.
}
\description{
Compare monthly issue rate across all four sources (\code{pypi}, \code{npm},
\code{joss}, \code{ropensci}), for one popularity stratum. Note that "stratum" is
relative to each source's own distribution (see \code{issue_rate_tbl()}/
\code{popularity_strata()}) - e.g. pypi's Q4 download count and joss's Q4
star count aren't the same absolute popularity, just each source's own
top quarter. A source with no data yet for the requested window (e.g.
not fully fetched - see \code{analysis-plan.md}) just contributes no line,
rather than erroring.
}
