% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community-breadth.R
\name{author_interval_trend_tbl}
\alias{author_interval_trend_tbl}
\title{Rolling geometric-mean wait time between consecutive first-time authors,
by source and popularity stratum}
\usage{
author_interval_trend_tbl(
  issue_authors_tbl,
  repo_tbl,
  source_name,
  n_strata = 4L,
  window = 12L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{source_name}{One of \code{repo_tbl$source} (\code{"pypi"}, \code{"npm"}, \code{"joss"},
\code{"ropensci"}).}

\item{n_strata}{Number of popularity strata.}

\item{window}{Trailing aggregation window, in months. Default 12: each
reported month's \code{n_metric}/\code{n_repo_months} sum that month and the
preceding 11.}

\item{date_start, date_end}{Date bounds on the analysis window;
\code{date_end} defaults to the start of the current month.}
}
\value{
A tibble with one row per (popularity stratum, month):
\code{popularity_stratum}, \code{month}, \code{n_events} (trailing sum of author
arrivals contributing an interval that month), \code{geo_mean_days} (the
\code{window}-month trailing geometric mean of \code{interval_days}, \code{NA} where
\code{n_events} is 0). Carries \code{window} and \code{source_name} as attributes.
}
\description{
Aggregates \code{author_interval_tbl()}'s event-level intervals into a
(popularity stratum x month) grid: each interval is binned by the
calendar month of its \code{event_time}, and reported as a \code{window}-month
trailing geometric mean of \code{interval_days}. A geometric (not arithmetic)
mean is used because wait times between authors are heavily
right-skewed - in a sparse stratum-month cell, a single repo that went
quiet for years would otherwise dominate an arithmetic mean of just a
handful of intervals. A handful of near-simultaneous arrivals
(\code{interval_days} at or near 0) are floored at one minute before logging,
since \code{log(0) = -Inf} would otherwise wreck that whole cell's geometric
mean rather than just pulling it down.
}
\examples{
\dontrun{
it <- author_interval_trend_tbl (issue_authors_tbl, repo_tbl, "pypi")
}
}
