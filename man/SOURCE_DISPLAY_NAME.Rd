% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-popularity.R
\name{SOURCE_DISPLAY_NAME}
\alias{SOURCE_DISPLAY_NAME}
\title{Proper-cased display forms of each source's internal (lowercase)
\code{POPULARITY_METRIC}/\code{repo_tbl$source} key, for anywhere a source name is
shown to a reader rather than matched against data (e.g. plot
annotations). "npm" is genuinely lowercase as a name, not an
abbreviation, so it's left as-is.}
\format{
A named character vector, one entry per source.
}
\usage{
SOURCE_DISPLAY_NAME
}
\description{
Proper-cased display forms of each source's internal (lowercase)
\code{POPULARITY_METRIC}/\code{repo_tbl$source} key, for anywhere a source name is
shown to a reader rather than matched against data (e.g. plot
annotations). "npm" is genuinely lowercase as a name, not an
abbreviation, so it's left as-is.
}
