% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-activity.R
\name{author_density_step_change_tbl}
\alias{author_density_step_change_tbl}
\title{Fold-change in distinct non-core author density, from a reference month
to now}
\usage{
author_density_step_change_tbl(
  issue_authors_tbl,
  repo_tbl,
  sources,
  contrib_threshold = 0.01,
  contrib_min = -Inf,
  window = 12L,
  ref_date = as.Date("2021-01-01")
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{sources}{Character vector of \code{source_name} values to include (see
\code{issue_rate_tbl()}).}

\item{contrib_threshold, window}{Passed to \code{author_density_tbl()}.}

\item{contrib_min}{As in \code{author_density_tbl()}.}

\item{ref_date}{Reference \code{Date} (first-of-month) to compare against the
latest available month.}
}
\value{
A tibble: \code{source}, \code{popularity_stratum}, \code{rate_ref},
\code{rate_latest}, \code{step_change}, \code{latest_month}.
}
\description{
The \code{author_density_tbl()} analogue of \code{step_change_tbl()}: for each of
several sources, compare each popularity stratum's author-density value
at a fixed reference month against its most recent value.
}
\examples{
\dontrun{
fc <- author_density_step_change_tbl (issue_authors_tbl, repo_tbl, c ("cran", "npm"))
plot_step_change (fc, SOURCE_DISPLAY_NAME, metric = "issues") +
    ggplot2::labs (title = "Distinct authors")
}
}
