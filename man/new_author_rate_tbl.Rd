% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-activity.R
\name{new_author_rate_tbl}
\alias{new_author_rate_tbl}
\title{New (non-founding) authors first appearing per repo-month, by source and
popularity stratum}
\usage{
new_author_rate_tbl(
  issue_authors_tbl,
  repo_tbl,
  source_name,
  n_strata = 4L,
  window = 12L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{source_name}{One of \code{repo_tbl$source} (\code{"pypi"}, \code{"npm"}, \code{"joss"},
\code{"ropensci"}).}

\item{n_strata}{Number of popularity strata.}

\item{window}{Trailing aggregation window, in months. Default 12: each
reported month's \code{n_metric}/\code{n_repo_months} sum that month and the
preceding 11.}

\item{date_start, date_end}{Date bounds on the analysis window;
\code{date_end} defaults to the start of the current month.}
}
\value{
A tibble with one row per (popularity stratum, month):
\code{popularity_stratum}, \code{month}, \code{n_metric} (trailing sum of new,
non-founding first-time authors), \code{n_repo_months}, \code{rate}. Carries
\code{metric = "issues"}, \code{window}, and \code{source_name} as attributes (no
\code{contrib_threshold}, since none applies here) so \code{plot_activity()}/
\code{plot_new_author_rate()} don't need to be told them again.
}
\description{
Community-expansion analogue of \code{author_density_tbl()}: rather than "how
many distinct authors are active this month", counts how many people are
showing up in a repository's issue tracker \emph{for the first time ever} -
a direct measure of whether a repo's community of interlocutors is still
growing, or has stalled to the same recurring faces. Each repo's very
first-ever issue author (typically the maintainer opening the repo's own
first issue) is excluded as a "founding" event rather than a new
arrival, since it isn't itself community growth. As with
\code{issue_rate_tbl()}/\code{author_density_tbl()}, the result is normalised by
repo-months of exposure and reported as a \code{window}-month trailing sum,
so a single burst of new signups doesn't read as a permanent step
change.
}
\details{
Author identity here isn't split by \code{contribution}/\code{contrib_threshold}
as most of this package's other rate tables are - someone who goes on to
become a heavy contributor is still a new arrival the month they first
show up, so every first-time author counts, core and non-core alike,
matching \code{solo_repo_share_tbl()}'s treatment of contribution rather than
\code{issue_rate_tbl()}'s.
}
\examples{
\dontrun{
na <- new_author_rate_tbl (issue_authors_tbl, repo_tbl, "pypi")
plot_activity (na) + ggplot2::labs (y = "New (non-founding) authors")
}
}
