% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyses.R
\name{fetch_repo_commits}
\alias{fetch_repo_commits}
\title{Fetch monthly commit counts (\code{github_commit_counts_by_month()}) for many
repos, batched and checkpointed to disk exactly like
\code{fetch_issue_authors()} above - same checkpoint/resume logic and the same
concurrent-batch execution via \code{progressify}/\code{futurize}, for the same
reason: GitHub's hourly rate limit is a cumulative budget, so the risk is
running through it too fast overall, not concurrency within one batch.}
\usage{
fetch_repo_commits(
  repo_urls,
  out_dir,
  batch_size = 50L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL
)
}
\arguments{
\item{repo_urls}{Character vector of repo URLs to fetch issue authors for.
Assumed already ordered by priority if it matters which get fetched
first (see \code{interlace_for_even_coverage()}).}

\item{out_dir}{Directory to read/write the CSV + done-list checkpoint files.}

\item{batch_size}{Repos fetched (concurrently) per checkpoint.}

\item{date_start, date_end}{Passed to \code{github_commit_counts_by_month()}.}
}
\value{
A tibble with columns \code{repo_url}, \code{month}, \code{n_commits} - the full
accumulated result, including rows from any previous run(s).
}
\description{
Fetch monthly commit counts (\code{github_commit_counts_by_month()}) for many
repos, batched and checkpointed to disk exactly like
\code{fetch_issue_authors()} above - same checkpoint/resume logic and the same
concurrent-batch execution via \code{progressify}/\code{futurize}, for the same
reason: GitHub's hourly rate limit is a cumulative budget, so the risk is
running through it too fast overall, not concurrency within one batch.
}
\examples{
repo_urls <- c (
    "https://github.com/ropensci/targets",
    "https://github.com/ropensci/drake"
)
\dontrun{
commit_counts_tbl <- fetch_repo_commits (repo_urls, "path/to/repo-data-out")
}
}
