% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{build_working_sample}
\alias{build_working_sample}
\title{Generate a working sample from a full population.}
\usage{
build_working_sample(
  downloads_tbl = NULL,
  top_n_head = 15000L,
  tail_size = 40000L,
  label = NULL
)
}
\arguments{
\item{downloads_tbl}{A tibble with at least \code{name} and \code{downloads} columns,
as returned by \code{pypi_downloads_full()} or \code{npm_downloads_full()}.}

\item{top_n_head}{Number of most-downloaded packages to include
deterministically.}

\item{tail_size}{Number of packages to randomly draw from the remaining long
tail (outside the head). Capped at the size of that tail.}

\item{label}{Optional string used to prefix a \code{cli} status message (e.g.
\code{"PyPI"} or \code{"npm"}); no message is printed if \code{NULL}.}
}
\value{
A tibble combining the head and tail samples, with the same columns
as \code{downloads_tbl}.
}
\description{
Build a working sample from a full (name, downloads)
population, stratified by download popularity: a deterministic head of the
\code{top_n_head} most-downloaded packages, plus a random draw of up to
\code{tail_size} packages from the remaining long tail.
}
