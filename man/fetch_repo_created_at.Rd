% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyses.R
\name{fetch_repo_created_at}
\alias{fetch_repo_created_at}
\title{Fetch each repo's GitHub creation timestamp (\code{github_repo_created_at()})
for many repos, batched and checkpointed to disk exactly like
\code{fetch_issue_authors()} - repos already recorded as done (in
\verb{<out_dir>/repo-created-at-done.rds}) are skipped, so re-running after an
interruption picks up where it left off. This exists to give the activity
analysis a true repo-months exposure denominator (see \code{analysis-plan.md})
rather than a flat repo-count one.}
\usage{
fetch_repo_created_at(repo_urls, out_dir, batch_size = 50L)
}
\arguments{
\item{repo_urls}{Character vector of repo URLs to fetch creation dates for.}

\item{out_dir}{Directory to read/write the CSV + done-list checkpoint files.}

\item{batch_size}{Repos fetched (concurrently) per checkpoint.}
}
\value{
A tibble with columns \code{repo_url}, \code{repo_created_at}.
}
\description{
Fetch each repo's GitHub creation timestamp (\code{github_repo_created_at()})
for many repos, batched and checkpointed to disk exactly like
\code{fetch_issue_authors()} - repos already recorded as done (in
\verb{<out_dir>/repo-created-at-done.rds}) are skipped, so re-running after an
interruption picks up where it left off. This exists to give the activity
analysis a true repo-months exposure denominator (see \code{analysis-plan.md})
rather than a flat repo-count one.
}
