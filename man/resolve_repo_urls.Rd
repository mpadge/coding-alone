% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{resolve_repo_urls}
\alias{resolve_repo_urls}
\title{Resolve GitHub repo URLs for a working sample, and filter down to the
packages for which one was found.}
\usage{
resolve_repo_urls(working_sample = NULL, repo_urls_fn = NULL)
}
\arguments{
\item{working_sample}{A tibble with at least \code{name} and \code{downloads}
columns, as returned by \code{build_working_sample()}.}

\item{repo_urls_fn}{A function taking a character vector of package names
and returning a character vector of the same length, with a resolved
GitHub repo URL or \code{NA} for each.}
}
\value{
A tibble with \code{name}, \code{downloads}, and \code{repo_url} columns,
filtered to rows with a non-missing \code{repo_url}.
}
\description{
Applies \code{repo_urls_fn} to the \code{name} column of
\code{working_sample} (e.g. \code{pypi_repo_urls_many()} or \code{npm_repo_urls_many()}),
then drops rows for which no GitHub repo URL could be resolved. Shared
post-processing step for both the PyPI and npm working samples.
}
