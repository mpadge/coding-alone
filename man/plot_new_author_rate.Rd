% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_new_author_rate}
\alias{plot_new_author_rate}
\title{Plot new (non-founding) author arrival rate, across sources and strata}
\usage{
plot_new_author_rate(
  issue_authors_tbl,
  repo_tbl,
  source_display = NULL,
  n_strata = 4L,
  window = 12L,
  date_start = as.Date("2015-01-01"),
  date_end = NULL,
  start_year = NULL
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{source_display}{Named character vector as in \code{plot_fold_change()}.}

\item{n_strata, window, date_start, date_end}{Passed to each source's
\code{new_author_rate_tbl()} call.}

\item{start_year}{Optional year to crop the plotted window to, as in
\code{plot_activity()} - display-only, doesn't affect the underlying
repo-months/rate calculations.}
}
\value{
A ggplot object.
}
\description{
Calls \code{new_author_rate_tbl()} for every source in \code{POPULARITY_METRIC} and
row-binds the results, then plots each source's trailing-window rate
over calendar time, one line per popularity stratum, faceted by source
with a free y-scale per facet (sources sit on very different absolute
rates, as in \code{plot_cohort_age()}). Unlike \code{plot_fold_change()}/
\code{plot_activity_by_source()}, this builds its own multi-source table
internally rather than taking one as \code{tbl} - \code{new_author_rate_tbl()}
itself is single-source, matching \code{issue_rate_tbl()}/
\code{author_density_tbl()}.
}
\examples{
\dontrun{
plot_new_author_rate (issue_authors_tbl, repo_tbl, SOURCE_DISPLAY_NAME)
}
}
