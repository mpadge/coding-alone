% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{cohort_age_rate_tbl}
\alias{cohort_age_rate_tbl}
\title{Non-core rate by creation-year cohort and fixed repo age}
\usage{
cohort_age_rate_tbl(
  issue_authors_tbl,
  repo_tbl,
  sources,
  contrib_threshold = 0.01,
  metric = c("issues", "comments"),
  age_years = 0:1,
  date_start = as.Date("2010-01-01")
)
}
\arguments{
\item{issue_authors_tbl}{As returned by \code{fetch_issue_authors()}.}

\item{repo_tbl}{As returned by \code{build_repo_tbl()}.}

\item{sources}{Character vector of \code{source_name} values to include.}

\item{contrib_threshold, metric}{As in \code{issue_rate_tbl()}.}

\item{age_years}{Integer vector of ages (in whole years since repo
creation) to compute rates for. Default \code{0:1} (a repo's first and second
year of life).}

\item{date_start}{Earliest calendar month considered when accumulating
repo-month exposure (not an age cutoff).}
}
\value{
A tibble: \code{source}, \code{cohort_year}, \code{age_year}, \code{n_metric},
\code{n_repo_months}, \code{rate}.
}
\description{
For each source, bin non-core issue activity by (creation-year cohort,
age-in-years-since-creation) instead of by (popularity stratum, calendar
month). This is the age/period/cohort disentangling trick: fixing "age"
(e.g. a repo's first or second year of life) while letting "cohort"
(the calendar year that age happened to fall in) vary isolates a shared
calendar-time effect from a repository's own maturation curve. Pure
maturation predicts roughly constant rates across cohorts at a fixed age;
a shared calendar-time break instead predicts a monotonic decline in that
fixed-age rate as cohort year increases.
}
\examples{
\dontrun{
cohort_tbl <- cohort_age_rate_tbl (issue_authors_tbl, repo_tbl, "cran")
plot_cohort_age (cohort_tbl)
}
}
