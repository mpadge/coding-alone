% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_cohort_age}
\alias{plot_cohort_age}
\title{Plot non-core rate by creation-year cohort and fixed repo age}
\usage{
plot_cohort_age(tbl, source_display = NULL, min_repo_months = 30)
}
\arguments{
\item{tbl}{As returned by \code{cohort_age_rate_tbl()}.}

\item{source_display}{Named character vector as in \code{plot_step_change()}.}

\item{min_repo_months}{Cells with less exposure than this are dropped
before plotting, since a handful of repo-months makes for an unstable
rate estimate.}
}
\value{
A ggplot object.
}
\description{
Plot \code{cohort_age_rate_tbl()} output: rate (log10 y-axis) against
creation-year cohort, one line per fixed age, faceted by source with a
free y-scale per facet (sources sit on very different absolute rate
scales, and unlike the main cross-source plots this one isn't trying to
compare sources against each other - only cohorts within a source).
}
\examples{
\dontrun{
cohort_tbl <- cohort_age_rate_tbl (issue_authors_tbl, repo_tbl, "cran")
plot_cohort_age (cohort_tbl, SOURCE_DISPLAY_NAME)
}
}
